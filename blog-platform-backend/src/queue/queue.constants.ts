// Queue name — single source of truth used by both producer and consumer
export const BLOG_SUMMARY_QUEUE = 'blog-summary';

// Job name — allows multiple job types on the same queue in future
export const GENERATE_SUMMARY_JOB = 'generate-summary';
